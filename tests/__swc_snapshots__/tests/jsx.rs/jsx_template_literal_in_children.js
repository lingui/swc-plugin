import { Trans as Trans_ } from "@lingui/react";
<Trans_ {.../*i18n*/ {
    __lingui__: true,
    id: "pB1yir",
    values: {
        foo: foo,
        bar: bar
    },
    message: "Hello {foo} and {bar}"
}}/>;

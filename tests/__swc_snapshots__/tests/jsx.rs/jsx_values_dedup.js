import { Trans as Trans_ } from "@lingui/react";
<Trans_ {.../*i18n*/ {
    __lingui__: true,
    id: "LyhmWJ",
    values: {
        foo: foo,
        bar: bar
    },
    message: "Hello {foo} and {foo} {bar}"
}}/>;

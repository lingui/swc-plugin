import { Trans as Trans_ } from "@lingui/react";
<Trans_ {.../*i18n*/ {
    id: "+hE+5/",
    values: {
        count: count
    },
    message: "{count, plural, one {{count} items} other {...}}"
}}/>;

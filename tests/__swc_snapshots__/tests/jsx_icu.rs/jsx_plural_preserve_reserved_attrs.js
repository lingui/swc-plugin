import { Trans as Trans_ } from "@lingui/react";
<Trans_ {.../*i18n*/ {
    id: "8I55rI",
    values: {
        count: count
    },
    message: "{count, plural, one {...} other {...}}",
    context: "Message Context"
}} render={(v)=>v}/>;

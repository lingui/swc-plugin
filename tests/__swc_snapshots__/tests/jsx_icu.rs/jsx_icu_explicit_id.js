import { Trans as Trans_ } from "@lingui/react";
<Trans_ {.../*i18n*/ {
    __lingui__: true,
    id: "plural.id",
    values: {
        count: count
    },
    message: "{count, plural, one {Message} other {Messages}}"
}}/>;
